Python (as prototyping) and C implementation of alpha bleeding

Alpha bleeding is setting the color of invisible pixels to the color of the nearest visible pixels, so that when interpolation occurs between the color of an invisible pixel and a visible pixel it doesn't look jarring.
