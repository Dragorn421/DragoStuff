wip port of Z64Utils to avalonia (from windows forms thing)

tip: copying versions isn't part of csproj because I haven't figure out what to do with versions data yet
if using vs code, you can change the debug launch config with `"preLaunchTask": "build_and_cp_versions",`
and create task build_and_cp_versions as:
```json
{
    "label": "build_and_cp_versions",
    "dependsOn": "build",
    "command": "cp -r -t bin/Debug/net6.0 Z64Utils-Config-master/versions",
    "type": "shell",
}
```

tip: run z64utils with arguments to directly open something
"args": "--rom /home/dragorn421/Documents/oot/baserom.z64 --object-analyzer object_box",
if using vscode, cf launch.json

build instructions on ubuntu (iirc):
apt install dotnet-sdk-7.0
dotnet build
see also: the GHA config file under .github/

Ubuntu 24.04:
https://learn.microsoft.com/en-us/dotnet/core/install/linux-ubuntu#ubuntu-net-backports-package-repository
add-apt-repository ppa:dotnet/backports
apt install dotnet-sdk-6.0
dotnet build
